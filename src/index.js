import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import Routes from './routes/Routes';
const el = document.getElementById('dashboard');

ReactDOM.render(<Routes/>, document.getElementById('dashboard'));